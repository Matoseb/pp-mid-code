function tonDessin(t) { // t = valeur normalisée (entre 0 et 1)
  
  // YOUR CODE START ——————————————————————————————————————————————————————————————————————
  
  // background(255, 10);
  // noFill()
  // stroke('black');
  // strokeWeight(10);
  // rectMode(CENTER)
  // rect(width/2, height/2, width * t, height * t);


  
  
  // YOUR CODE END ——————————————————————————————————————————————————————————————————————
}